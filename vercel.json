{
  "framework": "nextjs",
  "buildCommand": "payload migrate:create --name init --force-accept-warning && payload migrate:fresh --force-accept-warning && next build",
  "installCommand": "npm install"
}
