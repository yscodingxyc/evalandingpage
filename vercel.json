{
  "framework": "nextjs",
  "buildCommand": "payload generate:importmap && payload migrate:create --name init --force-accept-warning && payload migrate --force-accept-warning && next build",
  "installCommand": "npm install"
}
